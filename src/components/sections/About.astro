---
import { Icon } from 'astro-icon/components';
import profile from '../../data/profile.json';

const yearsExperience = new Date().getFullYear() - profile.careerStartYear;
---

<section id="about" class="py-24 px-4">
  <div class="max-w-6xl mx-auto">
    <div class="stagger-children">
      <h2 class="text-3xl sm:text-4xl font-bold text-text mb-12 flex items-center gap-4">
        <span class="text-accent">//</span>
        About Me
      </h2>

      <div class="grid md:grid-cols-3 gap-12 items-start">
        <!-- Photo placeholder -->
        <div class="md:col-span-1" data-animate>
          <div class="aspect-square rounded-2xl bg-surface border border-border flex items-center justify-center overflow-hidden">
            <!-- Replace with actual photo -->
            <div class="text-center text-text-muted p-8">
              <Icon name="lucide:user" class="w-24 h-24 mx-auto mb-4 opacity-50" />
              <p class="text-sm">Photo placeholder</p>
            </div>
          </div>
        </div>

        <!-- Bio content -->
        <div class="md:col-span-2 space-y-6" data-animate>
          {profile.bio.map((paragraph) => (
            <p class="text-text-muted text-lg leading-relaxed">
              {paragraph}
            </p>
          ))}

          <!-- Quick stats -->
          <div class="grid grid-cols-2 sm:grid-cols-3 gap-4 pt-6">
            {profile.stats.map((stat) => (
              <div class="bg-surface rounded-lg p-4 border border-border">
                <div class="text-2xl font-bold text-accent">{stat.value === 'YEARS_EXPERIENCE' ? `${yearsExperience}+` : stat.value}</div>
                <div class="text-text-muted text-sm">{stat.label}</div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
